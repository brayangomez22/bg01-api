# BG-01 API — environment
# Copy to .env and fill in. Never commit .env.

# HTTP server
PORT=8080

# --- The following are introduced in later phases (kept here as a roadmap) ---

# SQLite file path (Phase 1)
# DB_PATH=bg01.db

# Single admin auth (Phase 2)
# ADMIN_PASSWORD_HASH=
# SESSION_SECRET=

# Frontend origin allowed to read the export endpoint / CORS (Phase 2)
# ALLOWED_ORIGIN=https://brayangomez.dev
